#! /bin/bash

. /opt/mylogin/stocks

base="/root/github/diamond_hands/cron"

{
if test "$1" = "EOD"
	then
		${base}/end_of_day
	else
		${base}/pre-get_prices
		${base}/get_price
		${base}/update_batches
	fi
} 2>&1 | logger -t get_price -i
